/** @type {import('next').NextConfig} */
const nextConfig = {
    // Strict mode on dev mode break the behavior of the moving David
    // on the home page
    reactStrictMode: false
}

module.exports = nextConfig
