import styles from './picture.module.css'

interface Props {
  height: number,
  width: number,
  hover: boolean,
}

export default function Picture(props: Props) {
  return (
    <div>
        
    </div>
  )
}
